Kelola - Money Management 

Install Library for Export Excell
"composer require maatwebsite/excel"
or use librabry up to date.

Migration Database
XAMPP 
- Apache
- MySQL

open ADMIN on MySQL
create a new database with name "kelola"
migrate on terminal
open on vscode > new terminal > make sure you on the right file or directory file > write "php artisan migrate"

open the code on vscode or on terminal > write "php artisan serve"

open on browser with addrres on terminal 
default "127.0.0.1:8000" + "/register" for go to the register page.